enum InputType {
  TEXT = 'text',
  NUMBER = 'number',
  LONGTEXT = 'longtext',
  DATE = 'date',
  UPLOAD = 'upload',
  SELECT = 'select',
  SELECT_LOGO = 'select-logo',
  CONTENT_EDITOR = 'content-editor'
}

export default InputType;
